diff --git a/templates/index.html b/templates/index.html
index 8a256db..c19ab83 100644
--- a/templates/index.html
+++ b/templates/index.html
@@ -1,6 +1,7 @@
 <!doctype html>
 <html lang="en">
 <head>
+  <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <meta charset="utf-8">
   <title>KML Grid Generator</title>
   <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
@@ -12,6 +13,12 @@
 <body>
   <div class="container">

+    <style>
+      #draw_map, #preview_map {
+        width:100%;
+        height:500px;
+      }
+    </style>

     <h1>KML → Grid KML Generator</h1>

@@ -53,7 +60,7 @@
     <hr>
 
     <div class="info">
-      <p>Generated grid with <strong>{{ total_cells }}</strong> cells. <a href="{{ download_url }}">Download KML</a></p>
+      <p>✅ Generated <strong>{{ total_cells }}</strong> grid cells — <a href="{{ download_url }}">Download KML</a></p>
     </div>
 
-    <div id="map" style="height:600px;"></div>
+    <div id="preview_map"></div>
 
@@ -62,12 +69,21 @@
       var map = L.map('map').setView([20,78], 5);
       var osm = L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
           maxZoom: 19, attribution: '© OpenStreetMap'
-      }).addTo(map);
+      });

-      // load kml
-      omnivore.kml("{{ preview_url }}").on('ready', function() {
-          map.fitBounds(this.getBounds());
-      }).addTo(map);
+      var preview = L.map("preview_map");
+      osm.addTo(preview);
+
+      // Load generated grid KML
+      omnivore.kml("{{ preview_url }}").on("ready", function() {
+         preview.fitBounds(this.getBounds());
+         setTimeout(() => preview.invalidateSize(true), 400);
+      }).addTo(preview);
+
     </script>
     {% endif %}

diff --git a/static/style.css b/static/style.css
index 95a3aa1..b3ac19d 100644
--- a/static/style.css
+++ b/static/style.css
@@ -1 +1,57 @@
-/* EMPTY: Broken */
+/* ✅ FIXED: Full CSS for map scaling & layout */
+
+body {
+  background:#f5f5f5;
+  font-family:Arial, sans-serif;
+  padding:20px;
+}
+
+.container {
+  max-width:900px;
+  margin:0 auto;
+  background:white;
+  padding:20px;
+  border-radius:10px;
+  box-shadow:0 0 10px rgba(0,0,0,0.1);
+}
+
+h1 {
+  margin-bottom:20px;
+  text-align:center;
+}
+
+form .row {
+  margin:15px 0;
+}
+
+label {
+  font-weight:bold;
+}
+
+button {
+  padding:10px 20px;
+  background:#007bff;
+  color:white;
+  border:none;
+  border-radius:5px;
+  cursor:pointer;
+}
+
+button:hover {
+  background:#0056b3;
+}
+
+
+/* ✅ Map containers */
+#preview_map {
+  width:100%;
+  height:600px;
+  border:1px solid #ccc;
+  margin-top:15px;
+  border-radius:6px;
+}
+
+/* ✅ Mobile responsiveness */
+@media(max-width:600px){
+  #preview_map { height:350px; }
+}
